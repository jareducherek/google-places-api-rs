datasource db {
    provider = "postgresql"
    url      = env("SQL_DB_URL")
}

generator client {
    // Corresponds to the cargo alias
    provider = "cargo prisma"
    // The location to generate the client. Is relative to the position of the schema
    output   = "../src/prisma.rs"
}

model Place {
    id   String @id
    name String
}
